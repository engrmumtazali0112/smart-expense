version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=change-this-in-production
      - DATABASE_URL=sqlite:///./smart_expense.db
    volumes:
      - .:/app
